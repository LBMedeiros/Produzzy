# Fallback para plataformas que leem Procfile (Render, Railway, Heroku...).
# O caminho recomendado no Render é o render.yaml na raiz do repositório.
release: alembic upgrade head
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
